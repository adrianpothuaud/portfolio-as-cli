import { appColors } from '@/constants/colors'
import styled from 'styled-components'

export const BeforePromptCurrentFolderWrapper = styled.div`
  color: ${appColors['Forest Green Crayola']};
  margin-right: 8px;

  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
`